export const environment = {
  production: true,
  // TODO: replace with the deployed backend URL once the Railway service is live (Fase 3 del plan).
  apiUrl: "https://REPLACE-WITH-RAILWAY-BACKEND-URL",
};
